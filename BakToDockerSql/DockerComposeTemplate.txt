version: '3.8'

services:
  sqlserver:
    user: root
    build:
      context: .
      dockerfile: Dockerfile
    container_name: localsql
    ports:
      - "{port}:{port}"
    volumes:
      - sqlvolume:/var/opt/mssql
    environment:
      MSSQL_SA_PASSWORD: {sqlPassword}
      ACCEPT_EULA: "Y"

volumes:
  sqlvolume:
